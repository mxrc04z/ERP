# MongoDB Atlas access.
# Copy this file to .env and fill in the values:  cp .env.example .env (or copy manually).
# NEVER commit the real .env — it contains credentials.

MONGODB_USERNAME=
MONGODB_PASSWORD=
MONGODB_URI=

# API server port
PORT=4000